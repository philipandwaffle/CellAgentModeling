using Assets.Agent.Refactor;
using Assets.Environment;
using System.Collections;
using UnityEngine;

namespace Assets.CASMTransmission {
    public class CASMTransmitter : MonoBehaviour {
        [SerializeField] CAController ca;
        [SerializeField] SMTicker sm;

        // Use this for initialization
        void Start() {

        }

        // Update is called once per frame
        void Update() {

        }
    }
}